FROM python:3.12-slim
WORKDIR /app

COPY servers/requirements-audit.txt .
RUN pip install --no-cache-dir -r requirements-audit.txt

COPY common/ ./common/
COPY servers/prompt_audit_server.py servers/audit_store.py ./servers/

ENV PYTHONPATH=/app
CMD ["python", "servers/prompt_audit_server.py"]
